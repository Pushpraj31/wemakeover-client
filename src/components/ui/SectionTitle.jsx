/* eslint-disable react/prop-types */

const SectionTitle = ({title}) => {
  return (
    <h2 className="font-semibold capitalize text-[32px] text-[#CC2B52] font-sans leading-[100%]">
      {title}
    </h2>
  );
}

export default SectionTitle